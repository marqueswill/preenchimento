### Dependências

Dev and client:
`pip install selenium pandas openpyxl PyPDF2 pyinstaller`


### Gerando executável

Acesse a pasta pelo terminal e execute:
`python -m PyInstaller --onefile --noconsole arquivo_exemplo.py`

Ou execute o comando na pasta raiz (deprecated, but useful?):
`./build_rgps.bat`

